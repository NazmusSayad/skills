IMPORTANT: NEVER run development servers or build the entire application.

IMPORTANT: NEVER write comments. If you write any comments, delete them before finishing the task.

IMPORTANT: MUST USE dedicated tools for working with files instead of bash, commands, Python, or scripts.
